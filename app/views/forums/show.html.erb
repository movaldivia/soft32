
<p id="notice"><%= notice %></p>

<h3>
  <strong>Topic:</strong>
  <%= @forum.topic %>
</h3>

<%= link_to "Crear publicacion", new_post_path(:forum_id => @forum.id)%> |
<%= link_to 'Edit', edit_forum_path(@forum) %> |
<%= link_to 'Back', '/' %>
<h1> POSTS </h1>

<table>
  <thead>
    <tr>
      <th>Topic</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <% if post.forum_id == @forum.id %>
          <td><%= post.body %></td>
          <td><%= link_to 'Show', post %></td>
          <% if user_signed_in? && current_user.administrator? %>
            <td><%= link_to 'Edit', edit_post_path(forum) %></td>
            <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <% end %>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>
